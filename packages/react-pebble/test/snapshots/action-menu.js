// Auto-generated by react-pebble compile-to-piu (v3 with state reactivity)
// Source: examples/action-menu.tsx rendered in Node mock mode.
//
// Regenerate: npx tsx scripts/compile-to-piu.ts > pebble-spike/src/embeddedjs/main.js

import {} from "piu/MC";
import PebbleButton from "pebble/button";


const sk0 = new Skin({ fill: "#000000" });
const sk1 = new Skin({ fill: "#ffffff" });
const st0 = new Style({ font: "bold 18px Gothic", color: "#000000" });
const st1 = new Style({ font: "bold 24px Gothic", color: "#000000" });
const st2 = new Style({ font: "bold 24px Gothic", color: "#ffffff" });

const bgSkin = new Skin({ fill: "#000000" });

class AppBehavior extends Behavior {
  onCreate(app) {
    const c = app.first;
    this.s0 = 0;
    this.s1 = "";
    this.sr2 = c.content("sr2");
  }
  onDisplaying(app) {
    this.refresh();
    new PebbleButton({ type: "down", onPush: (pushed, name) => { if (pushed) this.onButton({ button: name }); } });
    new PebbleButton({ type: "up", onPush: (pushed, name) => { if (pushed) this.onButton({ button: name }); } });
    new PebbleButton({ type: "back", onPush: (pushed, name) => { if (pushed) this.onButton({ button: name }); } });
  }
  onButton(e) {
    const name = e && e.button;
    if (name === "down") { this.s0 += 1; this.refresh(); }
    if (name === "up") { this.s0 -= 1; this.refresh(); }
    if (name === "back") { this.s1 = ""; this.refresh(); }
  }
  refresh() {
    if (this.sr2) this.sr2.skin = (this.s0 !== 0) ? sk0 : sk1;
  }
}

const WatchApp = Application.template(() => ({
  skin: bgSkin,
  Behavior: AppBehavior,
  contents: [
      new Container(null, { left: 0, right: 0, top: 0, bottom: 0, contents: [
        new Content(null, { left: 0, right: 0, top: 0, bottom: 0, skin: sk0 }),
        new Content(null, { left: 0, right: 0, top: 0, height: 28, skin: sk1 }),
        new Label(null, { top: 4, left: 4, width: 192, style: st0, string: "Actions" }),
        new Container(null, { left: 0, right: 0, top: 0, bottom: 0, contents: [
          new Content(null, { left: 0, right: 0, top: 30, height: 42, skin: sk1, name: "sr2" }),
          new Label(null, { top: 40, left: 12, width: 176, style: st1, string: "Reply" })
        ] }),
        new Container(null, { left: 0, right: 0, top: 0, bottom: 0, contents: [
          new Content(null, { left: 0, right: 0, top: 74, height: 42, skin: sk0 }),
          new Label(null, { top: 84, left: 12, width: 176, style: st2, string: "Dismiss" })
        ] }),
        new Container(null, { left: 0, right: 0, top: 0, bottom: 0, contents: [
          new Content(null, { left: 0, right: 0, top: 118, height: 42, skin: sk0 }),
          new Label(null, { top: 128, left: 12, width: 176, style: st2, string: "Mute" })
        ] }),
        new Container(null, { left: 0, right: 0, top: 0, bottom: 0, contents: [
          new Content(null, { left: 0, right: 0, top: 162, height: 42, skin: sk0 }),
          new Label(null, { top: 172, left: 12, width: 176, style: st2, string: "Open on Phone" })
        ] })
      ] })
  ],
}));

export default new WatchApp(null, { touchCount: 0, pixels: screen.width * 4 });
