// Auto-generated by react-pebble (Rocky.js backend for classic Pebble)
//
// Target: basalt (144x168)

var rocky = require('rocky');

var s0 = 0;

rocky.on('draw', function(event) {
  var ctx = event.context;

  ctx.fillStyle = '#000000';
  ctx.fillRect(0, 0, 200, 228);
  ctx.fillStyle = '#ffffff';
  ctx.beginPath();
  ctx.arc(100, 100, 4, 0, 2 * Math.PI, false);
  ctx.fill();
  ctx.fillStyle = '#ff0000';
  ctx.fillRect(100, 40, 2, 60);
  ctx.fillStyle = '#ffffff';
  ctx.font = '42px bold Bitham';
  ctx.textAlign = 'center';
  ctx.fillText("" + s0, 100, 170);
  ctx.fillStyle = '#00ffff';
  ctx.font = '24px Gothic';
  ctx.textAlign = 'center';
  ctx.fillText("" + s0, 100, 200);
  ctx.fillStyle = '#c0c0c0';
  ctx.font = '14px Gothic';
  ctx.textAlign = 'center';
  ctx.fillText('tilt: x=0 y=0', 100, 28);
});
