// Auto-generated by react-pebble (C SDK backend for classic Pebble)
// Target: basalt (144x168)

#include <pebble.h>

static Window *s_window;
static Layer *s_draw_layer;
static GBitmap *s_img_batman;


static void draw_proc(Layer *layer, GContext *ctx) {
  if (s_img_batman) {
    graphics_draw_bitmap_in_rect(ctx, s_img_batman, GRect(2, 49, 140, 70));
  }
}

static void window_load(Window *window) {
  Layer *root = window_get_root_layer(window);
  window_set_background_color(window, GColorWhite);

  s_draw_layer = layer_create(GRect(0, 0, 144, 168));
  layer_set_update_proc(s_draw_layer, draw_proc);
  layer_add_child(root, s_draw_layer);

  s_img_batman = gbitmap_create_with_resource(RESOURCE_ID_BATMAN);

  TextLayer *tl_0 = text_layer_create(GRect(0, 139, 144, 50));
  text_layer_set_background_color(tl_0, GColorClear);
  text_layer_set_text_color(tl_0, GColorBlack);
  text_layer_set_font(tl_0, fonts_get_system_font(FONT_KEY_GOTHIC_18));
  text_layer_set_text_alignment(tl_0, GTextAlignmentCenter);
  text_layer_set_text(tl_0, "Na na na na na na na na");
  layer_add_child(root, text_layer_get_layer(tl_0));

}

static void window_unload(Window *window) {
  gbitmap_destroy(s_img_batman);
  layer_destroy(s_draw_layer);
}

static void init(void) {
  s_window = window_create();
  window_set_window_handlers(s_window, (WindowHandlers) {
    .load = window_load,
    .unload = window_unload,
  });
  window_stack_push(s_window, true);
}

static void deinit(void) {
  window_destroy(s_window);
}

int main(void) {
  init();
  app_event_loop();
  deinit();
}
