{
  "name": "react-pebble-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm --filter react-pebble test",
    "typecheck": "pnpm --filter react-pebble typecheck"
  }
}
